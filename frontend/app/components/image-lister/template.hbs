<div class="img-lister">
  <div class="button">
    <a class="btn slider-button" {{action 'up'}}
      disabled="{{unless isUpEnabled "disabled"}}">Up</a>
  </div>

  {{#each images as |image|}}
  <div class="image">
   <img src="{{thumbnail image}}" {{action "choosePic" image }} />
   <div class="buttonbar"><a href="#" {{action 'choosePic' image }}>Use</a> |
     <a href="#" {{action "deleteImage" image}}>Delete</a>
   </div>
  </div>
  {{/each}}

  <div class="button">
    <a class="btn slider-button" {{action 'down'}}
      disabled="{{unless isDownEnabled "disabled"}}">Down</a>
  </div>
</div>

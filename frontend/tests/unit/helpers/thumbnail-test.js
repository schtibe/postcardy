import { thumbnail } from 'postcardy/helpers/thumbnail';
import { module, test } from 'qunit';

module('Unit | Helper | thumbnail');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = thumbnail([42]);
  assert.ok(result);
});
